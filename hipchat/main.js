/*
$(function() {
  $('.hipchat-ui').hipChatPanel({
    url: "https://www.hipchat.com/gU2LZswfh",
    timezone: "EST"
  });
});
*/

window.location.replace("https://www.hipchat.com/gU2LZswfh");

/*
function getQueryParams(qs) {
    qs = qs.split('+').join(' ');

    var params = {},
        tokens,
        re = /[?&]?([^=]+)=([^&]*)/g;

    while (tokens = re.exec(qs)) {
        params[decodeURIComponent(tokens[1])] = decodeURIComponent(tokens[2]);
    }

    return params;
}

var query = getQueryParams(document.location.search);
alert(query.room);
*/
